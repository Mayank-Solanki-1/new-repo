USE ecommerce_db;
-- Create an admin account manually: you can register via the app or insert a bcrypt hash of password 'admin123'.
-- Example (replace <bcrypt_hash> with a real hash if desired):
-- INSERT INTO users(name,email,password,role) VALUES ('Admin','admin@example.com','<bcrypt_hash>','admin');
